#!/bin/sh

# Optional: reapply the MT7603 one-stream rate mask if explicitly installed.
[ "$ACTION" = "add" ] || exit 0
[ "$INTERFACE" = "phy0-ap0" ] || exit 0

/usr/sbin/wifi-mcs-limit >/dev/null 2>&1 &
